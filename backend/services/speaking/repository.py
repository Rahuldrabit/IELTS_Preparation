from sqlalchemy.ext.asyncio import AsyncSession

# The Speaking service currently orchestrates AI Agents and doesn't have direct DB models.
# If speaking-specific tables are added (e.g. SpeakingAttempt), DB operations should go here.
